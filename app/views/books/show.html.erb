<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to t('.link_edit'), edit_book_path(@book) %> |
  <%= link_to t('.link_books'), books_path %>

  <%= button_to t('.button_destroy'), @book, method: :delete %>
</div>
